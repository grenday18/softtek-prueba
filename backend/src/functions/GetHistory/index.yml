GetHistory:
  name: get-history-${opt:stage}
  layers:
    - { Ref: MergedLambdaLayer }
  role:
    Fn::ImportValue: ${self:custom.infra}-ListHistoryRole
  events:
    - httpApi:
        method: get
        path: /api/v1/historial
  handler: ./src/functions/getHistory/handler.getHistory