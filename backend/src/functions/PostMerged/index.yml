PostMerged:
  name: post-merged-${opt:stage}
  layers:
    - { Ref: MergedLambdaLayer }
  role:
    Fn::ImportValue: ${self:custom.infra}-CreateMergedRole
  events:
    - httpApi:
        method: post
        path: /api/v1/almacenar
  handler: ./src/functions/postMerged/handler.postMerged